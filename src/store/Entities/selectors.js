import { get } from 'dot-prop-immutable'
import { values, map } from 'lodash'

// export const all = (state) => (type) => values(get(getEntities(), `${type}.byId`))
// export const allIds = (state) => (type) => values(get(getEntities(), `${type}.allIds`))
// export const byId = (state) => (type, id) => get(getEntities(), `${type}.byId.${id}`)
